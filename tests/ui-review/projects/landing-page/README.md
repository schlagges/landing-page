# Landing Page UI Review Adapter

Runs the generic UI review against this Landing Page project.

```bash
npm run ui-review
```

Defaults:

- Port: `4174`
- Base URL: `http://127.0.0.1:4174`

Overrides:

```bash
UI_REVIEW_LANDING_PAGE_PORT=4180 npm run ui-review
```
